---
phase: 01-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/components/ChartErrorBoundary.tsx
  - src/components/supply-chain/IndicatorChart.tsx
  - src/components/supply-chain/FoundryUtilizationChart.tsx
  - src/components/supply-chain/ServerLeadingIndicators.tsx
  - src/components/supply-chain/MemoryPriceIndicators.tsx
  - src/components/vcm/TotalWaferLineChart.tsx
  - src/components/vcm/DemandBarChart.tsx
  - src/components/vcm/DeviceStackedChart.tsx
  - src/components/vcm/VcmPage.tsx
  - src/components/customer-detail/ExternalComparison.tsx
  - src/components/customer-detail/MonthlyMetricsChart.tsx
  - src/components/customer-detail/EstimateTrendChart.tsx
  - src/components/customer-detail/IndustryMetricsPanel.tsx
autonomous: true
requirements:
  - FOUN-02
  - FOUN-03
must_haves:
  truths:
    - "A page-level rendering error shows a Korean error message with retry button instead of a white screen"
    - "A root layout error shows a fallback page with retry button"
    - "A single chart rendering error does NOT crash the entire dashboard -- only that chart shows an error fallback"
    - "Each of the 12 Recharts chart components is individually wrapped with an error boundary"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "Page-level error boundary with Korean UI and reset button"
      contains: "export default function Error"
    - path: "src/app/global-error.tsx"
      provides: "Root layout error boundary with own html/body"
      contains: "<html lang=\"ko\">"
    - path: "src/components/ChartErrorBoundary.tsx"
      provides: "Reusable chart-level error boundary class component"
      contains: "class ChartErrorBoundary"
  key_links:
    - from: "src/components/supply-chain/IndicatorChart.tsx"
      to: "src/components/ChartErrorBoundary.tsx"
      via: "import and JSX wrapping"
      pattern: "import ChartErrorBoundary from"
    - from: "src/components/vcm/DemandBarChart.tsx"
      to: "src/components/ChartErrorBoundary.tsx"
      via: "import and JSX wrapping"
      pattern: "<ChartErrorBoundary"
    - from: "src/components/customer-detail/ExternalComparison.tsx"
      to: "src/components/ChartErrorBoundary.tsx"
      via: "import and JSX wrapping"
      pattern: "<ChartErrorBoundary"
---

<objective>
Add React error boundaries at three levels: Next.js App Router error files (page-level and root layout) and a reusable ChartErrorBoundary wrapping all 12 Recharts chart components.

Purpose: Prevent any single rendering error from crashing the entire dashboard. Per D-01, each chart is individually protected so one failure does not affect others.
Output: `error.tsx`, `global-error.tsx`, `ChartErrorBoundary.tsx`, and 12 chart component files updated with error boundary wrapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fixes/01-CONTEXT.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error.tsx, global-error.tsx, and ChartErrorBoundary.tsx</name>
  <files>src/app/error.tsx, src/app/global-error.tsx, src/components/ChartErrorBoundary.tsx</files>
  <read_first>
    - src/app/layout.tsx (understand root layout structure, confirm no existing error.tsx)
    - src/app/page.tsx (understand page entry point)
    - src/app/globals.css (understand dark mode patterns for styling the fallback)
  </read_first>
  <action>
Create three new files:

**File 1: `src/app/error.tsx`** (per D-03)
- Must be a `'use client'` component
- Export default function `Error` with props `{ error: Error & { digest?: string }; reset: () => void }`
- CRITICAL: Use `reset` prop, NOT `unstable_retry` (Next.js 16.1.6 does not have unstable_retry)
- `useEffect` to log error via `console.error('[app] Page error:', error)`
- Render a centered container with:
  - `<h2>` with text "페이지를 표시할 수 없습니다"
  - `<p>` with text "일시적인 오류가 발생했습니다."
  - `<button>` with text "다시 시도" that calls `reset` on click
- Dark mode support via `dark:` Tailwind prefix classes: `dark:text-gray-100`, `dark:text-gray-400`
- Button styled with `bg-blue-600 hover:bg-blue-700 text-white rounded-md px-4 py-2 text-sm font-medium`

**File 2: `src/app/global-error.tsx`** (per D-03)
- Must be a `'use client'` component
- Export default function `GlobalError` with same props as error.tsx
- MUST include its own `<html lang="ko">` and `<body>` tags (it replaces root layout)
- Use inline styles or minimal Tailwind for the body: `className="flex min-h-screen items-center justify-center bg-gray-50"`
- Render centered `<h2>` "시스템 오류가 발생했습니다" and retry button "다시 시도"
- Log error via `console.error('[app] Global error:', error)` in useEffect

**File 3: `src/components/ChartErrorBoundary.tsx`** (per D-01, D-02)
- Must be a `'use client'` component
- Export default class `ChartErrorBoundary extends Component<Props, State>`
- Props interface: `{ children: ReactNode; chartName?: string }`
- State interface: `{ hasError: boolean }`
- `getDerivedStateFromError()` returns `{ hasError: true }`
- `componentDidCatch(error, errorInfo)` calls `console.error(\`[chart] ${this.props.chartName ?? 'Unknown'} render error:\`, error, errorInfo)`
- Fallback render: a div with `h-[320px]` height, centered content, rounded border, gray background
  - Text: "차트를 표시할 수 없습니다" (per D-02)
  - Retry button: "다시 시도" that calls `this.setState({ hasError: false })`
  - Dark mode classes: `dark:border-gray-700 dark:bg-gray-800 dark:text-gray-400`
- When `hasError` is false, render `this.props.children`
- Import: `import { Component, type ErrorInfo, type ReactNode } from 'react'`
  </action>
  <verify>
    <automated>cd /Users/keonsoopark/Documents/05_Dashborad/01_MI_Platform_Dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/error.tsx exists and contains `'use client'` on line 1
    - src/app/error.tsx contains `export default function Error`
    - src/app/error.tsx contains `reset` prop (NOT `unstable_retry`)
    - src/app/error.tsx contains "페이지를 표시할 수 없습니다"
    - src/app/global-error.tsx exists and contains `<html lang="ko">`
    - src/app/global-error.tsx contains `export default function GlobalError`
    - src/app/global-error.tsx contains "시스템 오류가 발생했습니다"
    - src/components/ChartErrorBoundary.tsx contains `export default class ChartErrorBoundary extends Component`
    - src/components/ChartErrorBoundary.tsx contains "차트를 표시할 수 없습니다"
    - src/components/ChartErrorBoundary.tsx contains `console.error`
    - src/components/ChartErrorBoundary.tsx contains `getDerivedStateFromError`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Three error boundary files created: error.tsx (page-level), global-error.tsx (root layout), ChartErrorBoundary.tsx (per-chart). All with Korean fallback UI, retry buttons, and dark mode support. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap all 12 Recharts chart components with ChartErrorBoundary</name>
  <files>
    src/components/supply-chain/IndicatorChart.tsx,
    src/components/supply-chain/FoundryUtilizationChart.tsx,
    src/components/supply-chain/ServerLeadingIndicators.tsx,
    src/components/supply-chain/MemoryPriceIndicators.tsx,
    src/components/vcm/TotalWaferLineChart.tsx,
    src/components/vcm/DemandBarChart.tsx,
    src/components/vcm/DeviceStackedChart.tsx,
    src/components/vcm/VcmPage.tsx,
    src/components/customer-detail/ExternalComparison.tsx,
    src/components/customer-detail/MonthlyMetricsChart.tsx,
    src/components/customer-detail/EstimateTrendChart.tsx,
    src/components/customer-detail/IndustryMetricsPanel.tsx
  </files>
  <read_first>
    - src/components/ChartErrorBoundary.tsx (the boundary just created in Task 1)
    - src/components/supply-chain/IndicatorChart.tsx (understand how ResponsiveContainer/LineChart are used -- pattern to replicate)
    - src/components/vcm/DemandBarChart.tsx (ComposedChart pattern)
    - src/components/customer-detail/ExternalComparison.tsx (multiple charts in one component)
    - src/components/vcm/VcmPage.tsx (inline BarChart usage)
    - All other 8 chart files listed above (understand each chart's ResponsiveContainer pattern)
  </read_first>
  <action>
For each of the 12 chart component files, add the ChartErrorBoundary wrapper around each `<ResponsiveContainer>` block (per D-01: individual chart wrapping, not page-level).

**For each file:**
1. Add import at the top (use `@/` alias per project convention):
   `import ChartErrorBoundary from '@/components/ChartErrorBoundary';`

2. Wrap each `<ResponsiveContainer>` block with `<ChartErrorBoundary chartName="ComponentName">...</ChartErrorBoundary>`

**Specific wrapping targets per file:**

| File | Chart(s) to wrap | chartName value |
|------|-----------------|-----------------|
| IndicatorChart.tsx | 2x LineChart inside ResponsiveContainer | "IndicatorChart" |
| FoundryUtilizationChart.tsx | 1x LineChart | "FoundryUtilizationChart" |
| ServerLeadingIndicators.tsx | 1x LineChart | "ServerLeadingIndicators" |
| MemoryPriceIndicators.tsx | 1x LineChart | "MemoryPriceIndicators" |
| TotalWaferLineChart.tsx | 1x LineChart | "TotalWaferLineChart" |
| DemandBarChart.tsx | 1x ComposedChart | "DemandBarChart" |
| DeviceStackedChart.tsx | 1x BarChart | "DeviceStackedChart" |
| VcmPage.tsx | 1x BarChart (inline) | "VcmCompanyChart" |
| ExternalComparison.tsx | 3x (LineChart + 2x ComposedChart) | "ExternalComparison-{type}" for each |
| MonthlyMetricsChart.tsx | 1x ComposedChart | "MonthlyMetricsChart" |
| EstimateTrendChart.tsx | 1x LineChart | "EstimateTrendChart" |
| IndustryMetricsPanel.tsx | 1x LineChart | "IndustryMetricsPanel" |

**Wrapping pattern example:**
```tsx
// BEFORE:
<ResponsiveContainer width="100%" height={320}>
  <LineChart data={data}>
    {/* ... */}
  </LineChart>
</ResponsiveContainer>

// AFTER:
<ChartErrorBoundary chartName="IndicatorChart">
  <ResponsiveContainer width="100%" height={320}>
    <LineChart data={data}>
      {/* ... */}
    </LineChart>
  </ResponsiveContainer>
</ChartErrorBoundary>
```

For ExternalComparison.tsx which has 3 separate charts, wrap EACH ResponsiveContainer individually with a unique chartName (e.g., "ExternalComparison-line", "ExternalComparison-composed1", "ExternalComparison-composed2").

Do NOT change any other logic, props, or styling in these files. Only add the import and the wrapping JSX.
  </action>
  <verify>
    <automated>cd /Users/keonsoopark/Documents/05_Dashborad/01_MI_Platform_Dashboard && grep -rl "ChartErrorBoundary" src/components/ | wc -l | xargs -I{} bash -c 'echo "Files with ChartErrorBoundary: {}"; if [ {} -ge 13 ]; then echo "OK"; else echo "FAIL: expected >= 13 files"; exit 1; fi' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 12 chart component files contain `import ChartErrorBoundary from '@/components/ChartErrorBoundary'`
    - All 12 chart component files contain at least one `<ChartErrorBoundary` JSX tag
    - src/components/customer-detail/ExternalComparison.tsx contains at least 3 `<ChartErrorBoundary` tags (3 separate charts)
    - grep -c "ChartErrorBoundary" across all 12 files returns >= 2 per file (import + at least one usage)
    - src/components/ChartErrorBoundary.tsx itself exists (13th file with ChartErrorBoundary reference)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 12 Recharts chart components individually wrapped with ChartErrorBoundary. Each chart failure is isolated -- one broken chart shows "차트를 표시할 수 없습니다" while others continue rendering. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `ls src/app/error.tsx src/app/global-error.tsx src/components/ChartErrorBoundary.tsx` -- all three files exist
3. `grep -rl "ChartErrorBoundary" src/components/` returns 13 files (1 definition + 12 consumers)
4. `grep -c "<ChartErrorBoundary" src/components/customer-detail/ExternalComparison.tsx` returns >= 3
5. `grep "unstable_retry" src/app/error.tsx src/app/global-error.tsx` returns 0 matches
</verification>

<success_criteria>
- error.tsx catches page-level errors with Korean UI and reset button
- global-error.tsx catches root layout errors with own html/body and Korean UI
- ChartErrorBoundary class component provides per-chart error isolation
- All 12 Recharts chart components are individually wrapped (per D-01)
- Fallback UI shows "차트를 표시할 수 없습니다" with retry button (per D-02)
- Dark mode supported in all error UIs
- npm run build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fixes/01-02-SUMMARY.md`
</output>
